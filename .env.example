# Credenciales server-side de la GitHub App. La clave privada admite PEM
# multilínea o saltos escapados como \\n. Nunca uses prefijo PUBLIC_ para estos datos.
GITHUB_APP_ID=123456
GITHUB_APP_INSTALLATION_ID=12345678
GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"
# Alternativa recomendada en Netlify para evitar problemas con valores
# multilínea. Si está definida, tiene prioridad sobre GITHUB_APP_PRIVATE_KEY.
# GITHUB_APP_PRIVATE_KEY_BASE64=LS0tLS1CRUdJTi...

# Fallback temporal obsoleto. Retíralo después de comprobar la GitHub App.
# GITHUB_TOKEN=github_pat_xxxxxxxxxxxxxxxxxxxx

# Repositorio donde se versiona el Markdown (public/images/ solo conserva legacy).
GITHUB_REPO=Noisk8/simposio-memorias-participativas

# Origen permitido para llamadas CORS desde el frontend.
SITE_URL=https://simposio-memorias-participativas.netlify.app

# Lista opcional de orígenes adicionales, separados por comas (previews autorizados).
ALLOWED_ORIGINS=https://deploy-preview-123--sitio.netlify.app

# Rama base protegida de los Pull Requests editoriales (por defecto main).
GITHUB_BRANCH=main

# ── Supabase Auth (paneles de administración) ─────────────────────────────────
# URL del proyecto (Dashboard → Settings → API).
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co

# Clave service_role (NUNCA exponer en el cliente ni en el repositorio).
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...

# Almacenamiento de binarios. Auth y el catálogo permanecen en Supabase.
# Activar garage solo después de configurar estas variables server-side.
# Las filas anteriores conservan su proveedor según su public_url registrada.
CMS_MEDIA_STORAGE_PROVIDER=supabase
S3_ENDPOINT=https://s3.example.org
S3_REGION=garage
S3_BUCKET=cms-media
S3_ACCESS_KEY_ID=replace-with-garage-access-key
S3_SECRET_ACCESS_KEY=replace-with-garage-secret
S3_FORCE_PATH_STYLE=true
S3_PUBLIC_BASE_URL=https://cms-media.example.org
# Solo GitHub Actions: bucket PRIVADO separado para respaldos cifrados.
# Usar una clave distinta con lectura de cms-media y lectura/escritura de cms-backups.
# S3_BACKUP_BUCKET=cms-backups

# HMAC opcional para anonimizar sujetos del rate limit distribuido. Si se omite,
# el backend usa SUPABASE_SERVICE_ROLE_KEY como clave con separación de dominio.
# RATE_LIMIT_HMAC_KEY=replace-with-at-least-32-random-bytes

# Versiones públicas de la URL y la anon key (seguras para el navegador).
PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...

# Los administradores iniciales se predeclaran en public.admin_emails.
# Aplica primero, en orden, las migraciones de supabase/migrations/.

# Correo de alta opcional. Sin estas variables la cuenta se crea igualmente y
# el panel muestra la contraseña temporal para comunicarla por otro canal.
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
# RESEND_FROM_EMAIL=panel@example.org

# Política server-side de imágenes. Todo archivo tiene un máximo absoluto de
# 2 MiB impuesto en backend, PostgreSQL y Storage.
# CMS_IMAGE_MAX_WIDTH=8000
# CMS_IMAGE_MAX_HEIGHT=8000
# CMS_IMAGE_MAX_PIXELS=40000000
# CMS_IMAGE_OUTPUT_MAX_WIDTH=2560
# CMS_IMAGE_OUTPUT_MAX_HEIGHT=2560
# CMS_IMAGE_WEBP_QUALITY=82

# Webhook de alertas (opcional): cada error 5xx de las Functions se envía como
# POST JSON {"text": "..."} a esta URL. Compatible con Slack y Discord.
# ALERT_WEBHOOK_URL=https://hooks.slack.com/services/...

# Opcionales: permiten reconciliar despliegues históricos. El runtime confirma
# su propio COMMIT_REF sin token. Si se usa, el token solo necesita leer el sitio.
# NETLIFY_SITE_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# NETLIFY_API_TOKEN=nfp_xxxxxxxxxxxxxxxxxxxx

# Build hook de Netlify limitado a main. La función scheduled-publish lo invoca
# cada día a las 05:05 UTC (00:05 Bogotá) para activar contenido con fecha futura
# vencida. Usa hooks separados por entorno: uno para producción (main) y otro para
# staging. Alterna el valor de SCHEDULED_BUILD_HOOK_URL según dónde corras.
# SCHEDULED_BUILD_HOOK_URL=https://api.netlify.com/build_hooks/xxxxxxxxxxxxxxxxxxxxxxxx

# Textos y versiones en un bucket PRIVADO de Garage (sin website).
# Activar después de desplegar el backend compatible.
CMS_CONTENT_STORAGE_PROVIDER=garage
S3_CONTENT_BUCKET=cms-content

# Alias HTTPS anteriores de medios, separados por comas; solo server-side.
# S3_LEGACY_PUBLIC_BASE_URLS=https://old-media.example.org
